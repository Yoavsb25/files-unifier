"""
CSV parsing constants (single source).
Used by utils.column_reader and core (via CsvSerialConstants) so CSV/encoding values are not duplicated.
"""

UTF_8_ENCODING = "utf-8"
CSV_SAMPLE_SIZE = 1024
DEFAULT_CSV_DELIMITER = ","
